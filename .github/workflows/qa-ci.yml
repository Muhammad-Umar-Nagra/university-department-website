name: QA CI Pipeline

on:
  push:
    branches:
      - release

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Validate HTML Files
        run: |
          echo "Checking HTML files..."
          ls *.html

      - name: Build Docker Image
        run: |
          docker build -t university-website-qa .